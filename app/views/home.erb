
  <h1>- Bookmarks -</h1>
  <h2>Home</h2>

    <ul id="links">
      <% @links.each do |link| %>
        <li><a href="<%=link.href %>"><%=link.title %></a></li>
      <% end %>
    </ul>

  <form action='/add_link' method="get">
    <div class="form-group">
      <input type="submit" class="btn btn-default" id="add_link" value="Add Link">
    </div>
  </form>
